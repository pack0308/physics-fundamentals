// kinematics-sim.js
function setup() {
  let canvas = createCanvas(600, 400);
  canvas.parent("kinematics-sim");
}

function draw() {
  background(230);
  fill(0);
  textAlign(CENTER, CENTER);
  text("Kinematics Simulation Placeholder", width / 2, height / 2);
}
