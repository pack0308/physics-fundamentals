
function setup() {
  let canvas = createCanvas(600, 400);
  canvas.parent("momentum-sim");
}

function draw() {
  background(240);
  fill(0);
  textAlign(CENTER, CENTER);
  textSize(20);
  text("Momentum Simulation Placeholder", width / 2, height / 2);
}
