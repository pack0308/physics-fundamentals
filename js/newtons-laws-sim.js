
function setup() {
  let canvas = createCanvas(600, 400);
  canvas.parent("newtonsim");
}

function draw() {
  background(240);
  fill(0);
  textAlign(CENTER, CENTER);
  textSize(20);
  text("Newtons-laws Simulation Placeholder", width / 2, height / 2);
}
